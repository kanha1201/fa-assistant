# Minimal requirements for Vercel deployment
# Only includes what's needed for the FastAPI server

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Core utilities (built-in libraries used, but keeping minimal)
python-dotenv>=1.0.0

